apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: oauth-server
  name: oauth-server
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: oauth-server
  strategy: {}
  template:
    metadata:
      labels:
        app: oauth-server
    spec:
      containers:
        - image: brewery/oauth-server:latest
          name: oauth-server
          env:
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  key: db-host
                  name: oauth-server-secrets-test
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  key: db-name
                  name: oauth-server-secrets-test
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  key: db-user
                  name: oauth-server-secrets-test
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: db-password
                  name: oauth-server-secrets-test
          imagePullPolicy: Always
          resources: {}
          ports:
            - containerPort: 8080
              protocol: TCP
      imagePullSecrets:
        - name: acr-secret
status: {}
